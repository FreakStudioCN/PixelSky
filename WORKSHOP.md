# PixelSky Workshop 课前检查与快速部署

## 课前准备

1. 确认灯板类型：两块 8×8 模块横向拼接时按实际模块选择“右起逐列”或“逐行蛇形”；一体式 16×8 面板选择“逐行同向”。数据 GPIO 按开发板接线填写（ESP32 SuperMini/C3 测试板为 GPIO8），使用稳定 5V 供电并与 ESP32 共地。
2. 准备与芯片匹配的 MicroPython `.bin`；在页面选择 ESP32、S2、S3 或 C3 型号及固件。
3. 运行 `scripts/setup.ps1`，再运行 `scripts/start.ps1`。
4. 插入一块设备，选择串口并点击“一键设备检查”。
5. 未安装 MicroPython 时，确认芯片型号后点击“擦除并烧录 MicroPython”。ESP32/S2 写入 `0x1000`，S3/C3 写入 `0x0`；烧录会清除设备内容。
6. 点击“红绿蓝灯板测试”，核对颜色顺序、供电和 GPIO。
7. 点击“课堂快速部署”，系统会先检查设备，再上传 runtime、配置和当前动画。

## 30 台设备课前流程

- 给设备贴编号，每次只连接一台，避免串口误选。
- 每台依次完成：设备检查 → RGB 灯板测试 → 快速部署 → 断电重启验证。
- 记录异常类别：无串口、无 MicroPython、灯色错误、矩阵方向错误、供电闪烁。
- 开课前连续播放测试动画至少 5 分钟；准备两台备用设备和数据 USB 线。

## 课堂快速恢复

- 页面能看到串口但检查失败：重新烧录固件，再快速部署。
- 检查通过但灯不亮：检查 GPIO2、共地和外部 5V 供电，再运行 RGB 测试。
- 画面模块错位：两块 8×8 模块确认模块拼接顺序和模块内部走线；一体式 16×8 面板确认选择“逐行同向”，硬件视图编号应从每行左侧连续递增。
- 只需换作品：使用“仅更新 animation.json”，无需重复烧录或部署 runtime。

> 烧录和灯板测试只允许操作页面扫描到的串口。固件烧录属于破坏性操作，应确认设备编号后执行。
